extends Node3D

# this'll handle how the server the executable will boot,
# if its a server it'll only boot as the server within a terminal,
# while if its the client we'll preload any assets, textures, whatever is required, etc.
